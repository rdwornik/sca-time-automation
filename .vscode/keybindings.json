// Recommended keybindings for SCA Time Automation project
//
// To apply these keybindings:
// 1. Press Ctrl+Shift+P
// 2. Type "Preferences: Open Keyboard Shortcuts (JSON)"
// 3. Copy the array items below into your global keybindings.json
//
// Note: Keybindings cannot be set per-project in VS Code,
// so this file serves as documentation only.

[
  {
    "key": "ctrl+q",
    "command": "workbench.action.terminal.toggleTerminal",
    "when": "!terminalFocus"
  }
]
